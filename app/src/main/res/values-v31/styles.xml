<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Same theme name; adds Android 12+ splash attributes -->
    <style name="Theme.LittleGenius" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>

        <!-- Use your full splash art -->
        <item name="android:windowSplashScreenAnimatedIcon">@drawable/splash_illustration</item>
        <item name="android:windowSplashScreenBackground">@color/splash_background</item>
    </style>
</resources>